%h1 Welcome to the Editor's Page
		- @articles.each do |article|
			= link_to(editor_article_path(article)) do
				.max-w-sm.rounded
					.px-6.py-4
						.font-bold.text-xl.mb-2
							= article.Title
							%p.text-grey-darker.text-base
			= link_to ('Home'), root_path


